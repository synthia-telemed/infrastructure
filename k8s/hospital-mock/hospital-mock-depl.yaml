apiVersion: apps/v1
kind: Deployment
metadata:
  name: hospital-mock-depl
  namespace: hospital-mock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hospital-mock-depl
  template:
    metadata:
      labels:
        app: hospital-mock-depl
    spec:
      containers:
        - name: hospital-mock-depl
          image: synthiatelemed.azurecr.io/hospital-it-system-mock:672a131e8058f732789cec8b58f2d02d332c8982
          resources:
            limits:
              memory: "256Mi"
              cpu: "150m"
            requests:
              memory: "100Mi"
              cpu: "10m"
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: hospital-mock-secret
                optional: false
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            failureThreshold: 3
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            failureThreshold: 3
            initialDelaySeconds: 20
            periodSeconds: 10

