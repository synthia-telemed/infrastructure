apiVersion: apps/v1
kind: Deployment
metadata:
  name: measurement-result-api-depl
  namespace: backend-api
spec:
  selector:
    matchLabels:
      app: measurement-result-api-depl
  template:
    metadata:
      labels:
        app: measurement-result-api-depl
    spec:
      containers:
        - name: measurement-result-api-depl
          image: synthiatelemed.azurecr.io/measurement-result-api:3d0e02d51b5b63365724c7ccf5c8dc8ee3b2cdfd
          resources:
            limits:
              memory: "256Mi"
            requests:
              cpu: 10m
              memory: "80Mi"
          envFrom:
            - secretRef:
                name: measurement-result-api-secret
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /api/healthcheck
              port: 3000
            failureThreshold: 5
            initialDelaySeconds: 10
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /api/healthcheck
              port: 3000
            failureThreshold: 5
            initialDelaySeconds: 10
            periodSeconds: 5
